# 개발표준지침서

프로젝트명 : AI 기반 항만 게이트 차량 출입 및 작업 관리 시스템

2026. 7.

에스트래픽 스마트 모빌리티 DX Academy

---

## 목차

1. 개요
2. S/W 아키텍처
   - 2.1 구성도
   - 2.2 구성요소
   - 2.3 구현 예제
3. UI 설계 가이드
4. 개발 보안 가이드
5. SQL 튜닝

---

## 1. 개요

본 프로젝트는 항만 게이트의 차량 출입, 작업지시, 컨테이너 및 야드 현황을 통합 관리하기 위한 웹 기반 항만 게이트 관리 시스템이다. 운송사, 기사, 관리자 역할에 따라 차량 등록, 작업지시 조회 및 처리, 게이트 출입 이력 관리, 야드 현황 조회 기능을 제공한다.

또한 AI 기반 번호판 인식 기능을 연동하여 차량 진입 시 번호판을 자동으로 인식하고, 인식 결과를 차량 및 작업 데이터와 연결함으로써 게이트 처리 업무의 정확성과 효율성을 높인다. 백엔드는 Spring Boot 기반 REST API로 구현하고, 프론트엔드는 Vue.js 기반 SPA로 구성하며, AI 번호판 인식 기능은 FastAPI 기반 별도 API 서버로 제공한다. 데이터는 PostgreSQL을 사용하여 저장 및 관리한다.

본 개발표준지침서는 프로젝트 개발 과정에서 적용할 소프트웨어 아키텍처, 개발 기술, 구현 방식, UI 설계 기준, 보안 기준, SQL 작성 및 튜닝 기준을 정의하여 개발 산출물의 일관성, 유지보수성, 확장성을 확보하는 것을 목적으로 한다.

---

## 2. S/W 아키텍처

### 2.1 구성도

본 시스템은 사용자 화면, 업무 처리 서버, 데이터베이스, AI 번호판 인식 서버로 구성된다.

```text
사용자 브라우저
  |
  | Vue.js / Vite
  v
Frontend
  |
  | REST API 요청
  v
Spring Boot Backend
  |                         |
  | MyBatis                 | Multipart 이미지 요청
  v                         v
PostgreSQL DB          FastAPI AI Server
                            |
                            | YOLO / OpenCV / CRNN
                            v
                       번호판 인식 결과 반환
```

### 2.2 구성요소

#### 2.2.1 프레임워크 및 주요 기술

| 구분 | 기술 | 설명 |
|---|---|---|
| Backend | Spring Boot | Java 기반 웹 애플리케이션 개발 프레임워크로, REST API 서버 구현에 사용한다. |
| Backend | Spring Security | 로그인, 인증, 인가 처리를 위한 보안 프레임워크로 JWT 기반 인증 처리에 사용한다. |
| Backend | MyBatis | SQL Mapper 기반 데이터 접근 프레임워크로, Java 코드와 SQL을 분리하여 DB 연동을 처리한다. |
| Frontend | Vue.js | 사용자 화면을 컴포넌트 기반으로 구현하기 위한 JavaScript 프레임워크이다. |
| Frontend | Vite | Vue 프로젝트의 개발 서버 및 빌드 도구로 사용한다. |
| Frontend | Pinia | Vue 애플리케이션의 상태 관리를 위해 사용한다. |
| Frontend | Vue Router | 사용자 역할별 화면 이동 및 라우팅 처리를 위해 사용한다. |
| Frontend | Leaflet | 야드맵 또는 위치 기반 화면 표현을 위해 사용하는 지도/공간 시각화 라이브러리이다. |
| Database | PostgreSQL | 차량, 사용자, 운송사, 컨테이너, 작업지시, 게이트 로그, 번호판 인식 결과 등 업무 데이터를 저장한다. |
| AI API | FastAPI | Python 기반 AI 번호판 인식 API 서버를 구현하기 위해 사용한다. |
| AI API | Uvicorn | FastAPI 애플리케이션 실행을 위한 ASGI 서버로 사용한다. |
| AI / Vision | Ultralytics YOLO | 차량 번호판 영역 검출 모델 구현 및 추론에 사용한다. |
| AI / Vision | OpenCV | 이미지 전처리, 번호판 영역 처리, 영상 관련 기능 구현에 사용한다. |
| AI / OCR | PyTorch | CRNN 기반 OCR 모델 학습 및 추론에 사용한다. |

#### 2.2.2 개발 환경 및 라이브러리 버전

| 구분 | name | version |
|---|---|---|
| Java | Java | 17 |
| Gradle | Gradle Wrapper | 8.14.5 |
| Spring Boot | org.springframework.boot | 3.5.16 |
| Spring Dependency Management | io.spring.dependency-management | 1.1.7 |
| Spring Boot | spring-boot-starter-web | Spring Boot 관리 버전 |
| Spring Boot | spring-boot-starter-security | Spring Boot 관리 버전 |
| Spring Boot | spring-boot-starter-validation | Spring Boot 관리 버전 |
| MyBatis | mybatis-spring-boot-starter | 3.0.5 |
| JWT | jjwt-api | 0.12.6 |
| JWT | jjwt-impl | 0.12.6 |
| JWT | jjwt-jackson | 0.12.6 |
| JSP | tomcat-embed-jasper | Spring Boot 관리 버전 |
| Lombok | lombok | Gradle/Spring Boot 관리 버전 |
| PostgreSQL | postgresql JDBC Driver | Spring Boot 관리 버전 |
| Node.js | node | ^22.18.0 또는 >=24.12.0 |
| Vue | vue | 3.5.39 |
| Vue Router | vue-router | 5.1.0 |
| Pinia | pinia | 3.0.4 |
| Leaflet | leaflet | 1.9.4 |
| Vite | vite | 8.1.0 |
| Vite Plugin | @vitejs/plugin-vue | 6.0.7 |
| Vite Devtools | vite-plugin-vue-devtools | 8.1.5 |
| Python | fastapi | requirements 기준 |
| Python | uvicorn | requirements 기준 |
| Python | ultralytics | requirements 기준 |
| Python | opencv-python | requirements 기준 |
| Python | torch | requirements 기준 |
| Python | torchvision | requirements 기준 |
| Python | numpy | requirements 기준 |
| Python | pandas | requirements 기준 |
| Python | scikit-learn | requirements 기준 |
| Python | matplotlib | requirements 기준 |
| Python | scipy | requirements 기준 |
| Python | pillow | requirements 기준 |
| Python | python-multipart | requirements 기준 |

※ Python 라이브러리는 `AI/dependencies.txt`에 버전이 고정되어 있지 않으므로, 실제 제출 시 팀 개발 환경에서 `pip freeze` 결과를 기준으로 버전을 확정한다.

#### 2.2.3 공통 모듈

공통 모듈은 애플리케이션 개발 시 반복적으로 사용되는 기능을 표준화하여 개발 생산성과 유지보수성을 높이기 위한 구성요소이다.

| 구분 | 주요 모듈 | 설명 |
|---|---|---|
| 인증/인가 | Auth, Security, JWT | 로그인, 회원가입, 사용자 인증, 권한 검증을 처리한다. |
| 사용자 관리 | User, Driver, Carrier | 관리자, 기사, 운송사 계정 및 관련 정보를 관리한다. |
| 차량 관리 | Vehicle | 차량 등록, 차량 상세 조회, 운송사/기사별 차량 조회 기능을 제공한다. |
| 작업 관리 | WorkOrder | 컨테이너 반출입 작업지시와 작업 상태 변경을 관리한다. |
| 게이트 관리 | GateLog | 차량의 게이트 출입 요청, 처리 결과, 출입 이력을 관리한다. |
| 번호판 인식 | PlateRecognition | 이미지 업로드, AI 서버 연동, 번호판 인식 결과 저장 및 수동 보정을 처리한다. |
| 예외 관리 | ExceptionLog | 인식 실패, 미등록 차량, 데이터 불일치 등 예외 상황을 기록하고 관리한다. |
| 야드 관리 | YardSector | 항만 야드 구역 정보 및 배치 현황을 관리한다. |
| 대시보드 | Dashboard | 작업 현황, 야드 현황, 최근 이벤트 등 통계 정보를 제공한다. |

### 2.3 구현 예제

#### 2.3.1 Controller 작성 예제

Controller는 클라이언트 요청을 수신하고 Service 계층을 호출하여 처리 결과를 반환한다. API 경로는 업무 도메인을 기준으로 명확하게 작성하며, 조회는 GET, 등록은 POST, 수정은 PUT/PATCH, 삭제는 DELETE 방식을 사용한다.

예시 대상:

- `VehicleController` : 차량 목록 조회, 차량 등록, 차량 수정 및 삭제
- `PlateRecognitionController` : 번호판 이미지 인식 요청, 수동 보정 요청
- `GateLogController` : 게이트 출입 처리 및 이력 조회

작성 기준:

- `@RestController`를 사용하여 JSON 응답을 기본으로 한다.
- 업무 도메인별로 `@RequestMapping` 경로를 분리한다.
- 파일 업로드 API는 `MultipartFile`을 사용한다.
- 요청 파라미터와 요청 본문은 DTO 객체로 전달한다.
- Controller에서는 복잡한 업무 로직을 작성하지 않고 Service에 위임한다.

#### 2.3.2 Service 작성 예제

Service는 업무 규칙을 처리하는 계층으로, Controller에서 전달받은 요청을 검증하고 Mapper 또는 외부 API를 호출하여 결과를 생성한다.

예시 대상:

- `VehicleService` : 차량 등록, 조회, 승인 상태 변경 처리
- `WorkOrderService` : 작업지시 생성, 상태 변경, 작업 이력 처리
- `PlateRecognitionService` : FastAPI AI 서버 호출, 번호판 인식 결과 저장, 수동 보정 처리

작성 기준:

- 트랜잭션이 필요한 업무는 Service 계층에서 관리한다.
- DB 접근은 Mapper를 통해 수행한다.
- 외부 AI API 호출 결과는 DTO로 변환하여 저장한다.
- 예외 상황은 명확한 메시지와 함께 처리한다.

#### 2.3.3 Mapper 작성 예제

Mapper는 MyBatis를 사용하여 데이터베이스 SQL을 실행하는 계층이다. SQL은 업무 도메인별 Mapper에 분리하여 작성하고, Java 객체와 DB 컬럼 간 매핑은 camelCase 규칙을 적용한다.

예시 대상:

- `VehicleMapper`
- `CarrierMapper`
- `DriverMapper`
- `WorkOrderMapper`
- `GateLogMapper`
- `PlateRecognitionMapper`

작성 기준:

- 조회 조건은 명확하게 작성한다.
- 반복 사용되는 SQL은 Mapper 단위로 관리한다.
- 대량 조회 화면은 페이징 조건을 적용한다.
- `SELECT *` 사용을 지양하고 필요한 컬럼만 조회한다.

#### 2.3.4 Vue 화면 작성 예제

프론트엔드는 Vue.js 기반 컴포넌트 구조로 작성한다. 화면은 관리자, 운송사, 기사 역할에 따라 분리하고, API 호출 로직은 `src/api` 영역에 작성하여 화면 컴포넌트와 분리한다.

예시 대상:

- 관리자: `AdminDashboardView`, `AdminGateLogsView`, `AdminPlateRecognitionView`
- 운송사: `CarrierDashboardView`, `CarrierWorkOrdersView`, `CarrierVehicleRegisterView`
- 기사: `DriverDashboardView`, `DriverGateStatusView`, `DriverVehiclesView`

작성 기준:

- 화면 이동은 Vue Router를 사용한다.
- 공통 상태는 Pinia Store에서 관리한다.
- API 호출 코드는 `src/api` 하위 모듈에 분리한다.
- 사용자에게 표시되는 용어와 버튼명은 화면 전체에서 일관성 있게 사용한다.
- 오류 발생 시 사용자가 이해할 수 있는 메시지를 제공한다.

#### 2.3.5 AI 번호판 인식 API 작성 예제

AI 서버는 FastAPI 기반으로 구성하며, Spring Boot 서버에서 전달한 차량 이미지를 수신하여 번호판 인식 결과를 JSON 형태로 반환한다.

API 기준:

| 항목 | 내용 |
|---|---|
| Health Check | `GET /api/health` |
| 번호판 인식 | `POST /api/plate-recognition` |
| 요청 형식 | multipart/form-data |
| 요청 데이터 | file |
| 응답 형식 | JSON |

AI 처리 흐름:

```text
이미지 업로드
  -> 번호판 영역 검출
  -> 이미지 전처리
  -> OCR 수행
  -> 후보 번호판 후처리
  -> 인식 결과 JSON 반환
```

작성 기준:

- API 경로는 Spring Boot 연동 경로와 일치하도록 관리한다.
- 모델 파일은 `models/` 디렉터리에서 관리한다.
- 번호판 검출 모델과 OCR 모델은 분리하여 유지보수한다.
- 인식 실패, 낮은 신뢰도, 후보 다중 발생 등의 경우 검토 필요 여부를 결과에 포함한다.

---

## 3. UI 설계 가이드

본 장에서는 항만 게이트 관리 시스템의 사용자 경험 품질을 확보하기 위한 UI 설계 기준을 정의한다. 사용자는 관리자, 운송사, 기사로 구분되며 각 역할별 주요 업무를 빠르고 정확하게 수행할 수 있도록 화면을 설계한다.

### 3.1 사용자 필요 정보와 기능 제공

사용자는 각자의 역할에 따라 필요한 정보가 다르므로, 화면에는 해당 역할의 주요 업무에 필요한 기능을 우선적으로 제공한다.

점검 항목:

- 관리자는 게이트 이력, 작업 현황, 예외 상황, 야드 현황을 확인할 수 있는가?
- 운송사는 작업지시, 차량 등록, 기사 승인 상태를 확인할 수 있는가?
- 기사는 본인 차량, 배정 작업, 게이트 처리 상태를 확인할 수 있는가?
- 번호판 인식 결과와 수동 보정 기능이 명확하게 제공되는가?

조치 사항:

- 사용자 역할별 메뉴를 분리한다.
- 대시보드에는 핵심 상태와 최근 이벤트를 우선 배치한다.
- 업무 처리에 필요한 버튼과 입력 항목만 화면에 노출한다.

### 3.2 작업 소요 시간 및 단계 최소화

사용자가 차량 등록, 작업지시 확인, 게이트 처리 상태 확인 등의 업무를 빠르게 수행할 수 있도록 화면 단계를 최소화한다.

점검 항목:

- 주요 기능에 2~3단계 이내로 접근할 수 있는가?
- 동일한 정보를 반복 입력하지 않는가?
- 검색, 필터, 상세 조회 기능이 적절히 제공되는가?

조치 사항:

- 목록 화면에는 검색 조건과 주요 상태 필터를 제공한다.
- 등록/수정 화면에는 필요한 입력 항목만 배치한다.
- 반복 입력이 필요한 항목은 기존 데이터를 활용하여 자동 입력한다.

### 3.3 직관적이고 일관성 있게 구현

화면의 용어, 버튼명, 색상, 상태 표시 방식은 전체 시스템에서 일관되게 사용한다.

점검 항목:

- 등록, 수정, 삭제, 승인, 반려 등의 버튼명이 일관적인가?
- 작업 상태와 게이트 상태가 동일한 기준으로 표시되는가?
- 오류 메시지가 사용자가 이해할 수 있는 문장으로 제공되는가?

조치 사항:

- 공통 라벨과 상태값은 설정 파일 또는 공통 모듈에서 관리한다.
- 동일 기능은 동일한 UI 패턴으로 구현한다.
- 화면별 스타일은 공통 CSS 기준을 따른다.

### 3.4 사용자가 원하는 방식으로 구현

사용자가 업무 상황에 따라 필요한 정보를 쉽게 찾고 조작할 수 있도록 검색, 필터, 상세보기 기능을 제공한다.

점검 항목:

- 차량번호, 운송사, 기사명, 작업 상태 등 주요 조건으로 검색 가능한가?
- 목록에서 상세 정보로 자연스럽게 이동할 수 있는가?
- 사용자가 잘못 입력한 내용을 수정할 수 있는가?

조치 사항:

- 주요 목록 화면에 검색 및 필터 기능을 제공한다.
- 상세 화면에는 관련 이력과 상태 정보를 함께 제공한다.
- 수동 보정이 필요한 번호판 인식 결과는 별도 상태로 관리한다.

### 3.5 사용자가 실수를 최소화할 수 있게 구현

삭제, 승인, 반려, 수동 보정 등 업무 결과에 영향을 주는 기능은 사용자가 실수하지 않도록 확인 절차를 제공한다.

점검 항목:

- 중요 작업 수행 전 확인 절차가 있는가?
- 필수 입력값 누락 시 즉시 안내하는가?
- 잘못된 데이터 입력 시 저장 전에 검증하는가?

조치 사항:

- 필수 입력값은 프론트엔드와 백엔드에서 모두 검증한다.
- 삭제 및 상태 변경 기능에는 확인 메시지를 제공한다.
- 번호판 인식 신뢰도가 낮은 경우 수동 검토 대상으로 분류한다.

### 3.6 모든 유형의 사용자 이용 가능 설계/구현

다양한 사용 환경에서도 주요 기능을 사용할 수 있도록 화면 가독성과 접근성을 고려한다.

점검 항목:

- 글자 크기와 색상 대비가 충분한가?
- 버튼과 입력 영역이 명확하게 구분되는가?
- 모바일 또는 작은 화면에서도 주요 정보가 확인 가능한가?

조치 사항:

- 상태값은 색상뿐 아니라 텍스트로도 표시한다.
- 주요 버튼은 충분한 크기와 간격을 유지한다.
- 반응형 레이아웃을 적용하여 화면 크기에 따라 정보를 재배치한다.

### 3.7 원하는 서비스와 정보를 쉽게 찾을 수 있게 구현

사용자가 원하는 정보를 빠르게 찾을 수 있도록 메뉴 구조와 화면 배치를 명확하게 구성한다.

점검 항목:

- 역할별 메뉴가 명확하게 분리되어 있는가?
- 자주 사용하는 기능이 상위 메뉴에 배치되어 있는가?
- 목록, 상세, 등록 화면 간 이동 흐름이 자연스러운가?

조치 사항:

- 관리자, 운송사, 기사별 라우터를 분리한다.
- 메뉴명은 업무 용어를 기준으로 작성한다.
- 대시보드에서 주요 업무 화면으로 이동할 수 있도록 구성한다.

---

## 4. 개발 보안 가이드

본 시스템은 사용자 계정, 차량 정보, 운송사 정보, 작업지시, 게이트 출입 이력 등 업무상 중요한 데이터를 처리하므로 개발 단계에서 보안 기준을 준수한다.

보안 기준:

- 사용자 비밀번호는 평문으로 저장하지 않고 암호화하여 저장한다.
- 로그인 인증은 JWT 기반으로 처리하며, 만료 시간을 설정한다.
- 관리자, 운송사, 기사 등 사용자 역할에 따라 접근 가능한 API와 화면을 제한한다.
- SQL Injection 방지를 위해 MyBatis 파라미터 바인딩을 사용한다.
- XSS 방지를 위해 사용자 입력값을 화면에 출력할 때 필요한 검증 및 이스케이프 처리를 수행한다.
- 파일 업로드 기능은 허용된 이미지 파일만 처리하며, 파일 크기를 제한한다.
- 서버 오류 메시지에 DB 접속 정보, 토큰, 내부 경로 등 민감한 정보를 노출하지 않는다.
- DB 접속 정보, JWT Secret 등 민감 정보는 운영 환경에서 환경변수 또는 별도 설정 파일로 분리한다.
- AI 서버와 백엔드 서버 간 연동 API는 허용된 경로와 요청 형식만 처리한다.
- 번호판 이미지 및 인식 결과는 업무 목적에 필요한 범위 내에서 관리한다.

웹 취약점 점검 항목:

- SQL Injection
- Cross Site Scripting(XSS)
- 파일 업로드 취약점
- 인증 및 권한 검증 누락
- 취약한 비밀번호 정책
- 민감 정보 노출
- 오류 처리 미흡
- 불필요한 HTTP Method 허용
- API 요청 파라미터 조작

---

## 5. SQL 튜닝

항만 게이트 관리 시스템은 차량 출입 이력, 작업지시, 번호판 인식 결과 등 지속적으로 증가하는 데이터를 처리하므로 안정적인 성능 유지를 위해 SQL 작성 및 튜닝 기준을 적용한다.

### 5.1 튜닝 목적

SQL 튜닝의 목적은 주요 화면과 API의 응답 속도를 안정적으로 유지하고, 대량 데이터 조회 시 불필요한 DB 부하를 줄이는 것이다.

수행 항목:

- 자주 조회되는 차량번호, 운송사 ID, 기사 ID, 작업 상태, 게이트 출입 일시 컬럼의 조회 성능 개선
- 목록 조회 API의 페이징 처리 적용
- 불필요한 전체 테이블 조회 방지
- JOIN 대상과 조건 최적화
- 번호판 인식 결과 및 게이트 로그 조회 성능 개선
- 대시보드 통계 쿼리의 집계 성능 개선

### 5.2 튜닝 절차

SQL 성능 튜닝은 다음 절차에 따라 수행한다.

```text
성능 저하 SQL 식별
  -> 실행 계획 확인
  -> 조회 조건 및 JOIN 구조 분석
  -> 인덱스 적용 여부 검토
  -> SQL 수정 및 테스트
  -> 개선 결과 확인
```

튜닝 대상 선정 기준:

- 실행 시간이 오래 걸리는 조회 SQL
- 대시보드, 게이트 로그, 작업지시 목록 등 자주 호출되는 SQL
- 대량 데이터가 누적되는 테이블을 조회하는 SQL
- 조건절에 인덱스가 적용되지 않는 SQL
- 불필요한 JOIN 또는 중복 조회가 발생하는 SQL

### 5.3 튜닝 항목

SQL 작성 기준:

- 필요한 컬럼만 명시적으로 조회하고 `SELECT *` 사용을 지양한다.
- 목록 조회 API는 페이징 조건을 적용한다.
- 검색 조건에 자주 사용되는 컬럼은 인덱스 적용을 검토한다.
- JOIN 조건에는 PK/FK 또는 인덱스 컬럼을 사용한다.
- 날짜 범위 조회 시 인덱스가 활용될 수 있도록 조건식을 작성한다.
- Mapper XML 또는 Mapper Interface의 SQL은 업무 도메인별로 분리하여 관리한다.

주요 인덱스 검토 대상:

| 테이블 | 검토 컬럼 | 목적 |
|---|---|---|
| users | user_id, role | 로그인 및 권한 조회 |
| vehicle | vehicle_id, plate_number, carrier_id, driver_id | 차량 조회 및 소유 관계 조회 |
| carrier | carrier_id | 운송사 정보 조회 |
| driver | driver_id, user_id | 기사 정보 조회 |
| work_order | work_order_id, vehicle_id, status | 작업지시 조회 및 상태별 검색 |
| gate_log | gate_log_id, vehicle_id, in_out_type, created_at | 게이트 출입 이력 조회 |
| plate_recognition | plate_recognition_id, plate_number, created_at | 번호판 인식 결과 조회 |
| exception_log | exception_log_id, status, created_at | 예외 상황 조회 |
| yard_sector | sector_id, sector_code | 야드 구역 조회 |

---

## 부록. 작성 시 교체하면 좋은 항목

제출 전 다음 항목은 팀 최종 기준으로 확인하여 수정한다.

- 표지의 작성 월
- 프로젝트 팀명 또는 조 번호
- 실제 아키텍처 구성도 이미지
- 실제 구현 코드 캡처
- Python 라이브러리 실제 버전
- DB 테이블명과 컬럼명이 최종 DB 정의서와 일치하는지 여부
- 화면 캡처가 필요한 경우 관리자/운송사/기사 대표 화면
